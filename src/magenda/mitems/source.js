import { BaseItemSource } from '@/magenda/core'

export class DefaultMItemSource extends BaseItemSource {

  constructor(name, configuration, sourceManager) {
    super(name, configuration, sourceManager)
  }
}
